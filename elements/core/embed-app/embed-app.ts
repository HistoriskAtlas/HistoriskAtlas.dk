@component("embed-app")
class EmbedApp extends App implements polymer.Element {

    created() {
        super.created();
    }

    ready() {
        super.ready();

        //alert(Common.embed);
    }
}

EmbedApp.register();

